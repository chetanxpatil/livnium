[project]
name = "livnium-native-pile"
version = "0.1.0"
description = "A minimal trainable Livnium pile embedded inside a neural model"
requires-python = ">=3.11"
dependencies = ["torch>=2.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
