# STATUS: archived (investigation concluded, negative result)

**Why it was attempted.** To test whether the collapse engine could predict
Rule 30's center column — a hard, well-defined target where success would
have been unambiguous evidence of predictive power.

**What happened.** It could not, once the test was made fair. Read
`honest_rule30_test.py` first, then `final_answer_rule30.py`; the two
`Rule30_*.docx` memos are the written assessment and full journey.

**What superseded it.** Nothing needed to — this was a self-contained
question with a clean negative answer. The honest-test methodology
(scaffolding vs. genuine prediction) carried forward into how benchmarks
are run in `benchmarks/`.

**What remains reusable.** The light-cone framing in `light_cone_rule30.py`
and the honest-test template. Negative result, kept on purpose.
