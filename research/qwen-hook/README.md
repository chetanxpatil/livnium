# Qwen hook — grade C

`qwen_probe.py` is a narrow integration experiment for observing collapse-related
signals inside a Qwen model. It is kept separate because it depends on an external
model stack and does not yet support a standalone Livnium result.
