# Archive — grade D / historical

Superseded code is preserved here so negative results and earlier directions stay
auditable without competing with maintained packages, trained models, or active
research.

- `cortex-v2/` — state-vector/MPS and early collapse bridge.
- `rule30/` — cellular-automaton prediction investigation.
- `experiments/` — loose standalone probes.
- `chat-legacy/` — superseded flattened-conversation preprocessing.
- `reached/` — large local staging snapshot, intentionally gitignored.

Archived code may run, but it receives no API-stability promise.
